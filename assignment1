program 

import re
import pytest


def example(text):
    a=re.findall('\.\w{2,3}',text)
    return (a[-1])
    

text = input("Enter website:")
b=example(text)
if b!=0:
    assert example(text)==b
    print (b)

else:
    print("Process failed!")
